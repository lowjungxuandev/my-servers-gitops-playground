apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kube-prometheus-stack
  namespace: argocd
spec:
  project: infrastructure
  source:
    repoURL: https://prometheus-community.github.io/helm-charts
    chart: kube-prometheus-stack
    targetRevision: 69.8.2
    helm:
      releaseName: kube-prometheus-stack
      values: |
        grafana:
          service:
            type: NodePort
            nodePort: 30300
          nodeSelector:
            kubernetes.io/hostname: infrastructure-node
          adminPassword: admin
        prometheus:
          service:
            type: NodePort
            nodePort: 30090
          prometheusSpec:
            nodeSelector:
              kubernetes.io/hostname: infrastructure-node
        alertmanager:
          alertmanagerSpec:
            nodeSelector:
              kubernetes.io/hostname: infrastructure-node
        kube-state-metrics:
          nodeSelector:
            kubernetes.io/hostname: infrastructure-node
        prometheus-node-exporter:
          nodeSelector:
            kubernetes.io/hostname: infrastructure-node
  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
